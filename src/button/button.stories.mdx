import { html, template } from "atomico";
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

<Meta title="Counter" />

export const Template = (args) => template(html`<fm-button>Button</fm-button>`);

# buttons

Este es un pequeño ejemplo...

<Story
    name="Unchecked"
    args={{
        label: 'Unchecked',
    }}>
    {Template}
</Story>
